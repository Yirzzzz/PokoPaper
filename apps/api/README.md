# Pokomon API

FastAPI backend for paper ingestion, overview generation, chat, recommendations, and memory.

## Run

```bash
pip install -e .
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
